import { ReponseCommentaire } from './reponse-commentaire';

describe('ReponseCommentaire', () => {
  it('should create an instance', () => {
    expect(new ReponseCommentaire()).toBeTruthy();
  });
});

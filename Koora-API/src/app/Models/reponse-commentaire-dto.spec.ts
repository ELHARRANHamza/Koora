import { ReponseCommentaireDto } from './reponse-commentaire-dto';

describe('ReponseCommentaireDto', () => {
  it('should create an instance', () => {
    expect(new ReponseCommentaireDto()).toBeTruthy();
  });
});
